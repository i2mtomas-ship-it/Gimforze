# EvolvAI v0.9.0

Versión candidata de integración visual y funcional.

## Actualización

Usa `UPGRADE_V0_9_0.ps1` desde la raíz del proyecto Android que ya funciona. El script reemplaza `lib`, `test` y `pubspec.yaml` y conserva la plataforma `android` existente.
